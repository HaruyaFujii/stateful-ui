---
"@behave-ui/cli": patch
---

Fix: Update DataFetch file list in CLI registry

Corrected the file list for DataFetch component to match actual implementation.
Removed non-existent cache-client.ts and added necessary files.